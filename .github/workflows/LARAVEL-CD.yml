name: LARAVEL_CD

on:
  push:
    branches:
    - main

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ftp.devtribe.my.id
        username: sko@devtribe.my.id
        password: ${{ secrets.ftp_password }}
        port: 21
